# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app"s APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Jetifier 已于 2026-09-11 关闭:全量依赖树仅剩 Picasso 传递的 support-annotations/exifinterface,
# 且经字节码扫描确认 Picasso 从未引用 ExifInterface(纯注解 + 死重),无需 support→androidx 改写
# 关闭后省去全依赖字节码改写,AGP 9 的 enableJetifier 弃用告警同时消除
# 关闭 AGP 9 默认的非传递 R 类:代码/布局直接引用了 media3、dkplayer 等依赖库中的资源
android.nonTransitiveRClass=false
# 关闭 AGP 9 默认的包名唯一性检查:
# 本地 :quickjs 模块与 wang.harlon.quickjs:wrapper-android 同包名(com.whl.quickjs.wrapper),为既有结构
android.uniquePackageNames=false
# 关闭 AGP 9 内置 Kotlin:其编译器版本固定为 AGP 自带的 2.2.10(不可覆盖),
# 无法满足 UI 改造指定的 Kotlin 2.4.10,故回退传统 KGP 插件接法(avbox-mobile-ui-spec §2)
android.builtInKotlin=false
# KGP 插件(org.jetbrains.kotlin.android)依赖旧 DSL 的 BaseExtension,
# 与上面 builtInKotlin=false 配套使用;两者均计划在 AGP 10 移除,届时需跟进官方接法
android.newDsl=false
#android.injected.build.abi=armeabi-v7a
# ↑ 历史遗留:AGP 注入式 ABI 仍不用;各模块 abiFilters 现为 arm64-v8a + armeabi-v7a(双 ABI 通用包)
#build on off
org.gradle.jvmargs=-Xmx4096m
org.gradle.caching=true
org.gradle.parallel=true
# Release 签名(密钥库位于根目录 .key/ 下)
RELEASE_STORE_FILE=.key/app-release.jks
RELEASE_KEY_ALIAS=key0
RELEASE_STORE_PASSWORD=123456
RELEASE_KEY_PASSWORD=123456

# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true
