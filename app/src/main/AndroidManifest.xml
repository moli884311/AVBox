<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- 旧存储权限在 Android 13+ 对 targetSdk>=33 的应用会被系统忽略,故限制到 12 及以下 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- Android 13+ 媒体权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Android 11+ 全部文件访问(浏览/备份/局域网共享任意路径,无系统弹窗,跳转设置页授权) -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <!--
        REQUEST_INSTALL_PACKAGES 保留(全项目零引用,属上游 TVBox 的历史遗留):一旦将来做
        "应用内更新"就必须用它,且它有实际风险(可触发安装未知来源应用),留着请知悉 —— 不需要就删本行。
        ACCESS_FINE_LOCATION / GET_TASKS 已于 2026-09-13 移除:DLNA 组播锁不需要定位,
        GET_TASKS 在 Android 5+ 已废弃,两者全项目零引用。
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ 带类型前台服务必需：mediaPlayback 类型服务的专用权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Android 13+ 通知为运行时权限:清单声明之外,还须 PermissionHelper.requestNotificationIfNeeded 显式申请 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android 17(API 37)起,targetSdk 37+ 的应用访问局域网(投屏/DLNA、clan://<ip>)被默认屏蔽,
         失败是静默超时(不抛异常);127.0.0.1 回环不受影响。运行时请求见 PermissionHelper -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />

    <!--
        2026-09-13 权限梳理(第二部分):删掉**依赖库清单**塞进来的无关权限。
        这些是随库合并进来的,不删就会进最终 APK —— 其中 MOUNT 属系统级高权限,
        上架会被追问用途,而本项目根本拿不到也用不到它。来源已用 manifest-merger 报告逐个核对:
          MOUNT_UNMOUNT_FILESYSTEMS : com.lzy.net:okgo:3.0.4(系统签名权限,第三方应用纯噪声)
          GET_TASKS                 : 上游 player 模块(Android 5+ 已废弃)
          READ_PHONE_STATE          : 上游 player 模块(ScreenUtils 已改造为不需要电话权限)
          ACCESS_FINE_LOCATION      : 上游/依赖(DLNA 组播锁不需要定位)
        注意:READ/WRITE_EXTERNAL_STORAGE 不在此列 —— 它们由本清单**合法声明**并带 maxSdkVersion=32,
        okgo 只是又声明了一遍(无 maxSdkVersion),清单合并会自动保留我们那份带上限的版本。
        ⚠️ 依赖升级/替换后请复查本清单是否仍有对应来源(okgo 若将来被移除,这几条可一并删掉)。
    -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" tools:node="remove" />
    <uses-permission android:name="android.permission.GET_TASKS" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" tools:node="remove" />

    <application
        android:name=".base.App"
        android:allowBackup="true"
        android:configChanges="orientation|keyboardHidden|screenSize"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">

        <!-- 方向策略见 BaseActivity.applyOrientationPolicy:sw<600dp 锁竖屏,>=600dp 放开。
             在此写死 portrait 会让平板被信箱化,且 API 37 起系统在大屏本就会忽略它。 -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.LivePlayActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize" />
        <activity
            android:name=".ui.activity.DetailActivity"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".ui.activity.MusicPlayerActivity"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".ui.activity.SearchActivity"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity
            android:name=".ui.activity.PartitionListActivity" />
        <activity
            android:name=".ui.activity.ThemeSettingsActivity" />
        <activity
            android:name=".ui.activity.ConfigManageActivity" />
        <activity
            android:name=".ui.activity.MoliysInterfaceActivity" />
        <activity
            android:name=".ui.activity.PlaySettingsActivity" />
        <activity
            android:name=".ui.activity.DanmuSettingsActivity" />
        <activity
            android:name=".ui.activity.PreferenceSettingsActivity" />
        <activity
            android:name=".ui.activity.PreloadSettingsActivity" />

        <service
            android:name=".dlna.DLNACastService"
            android:exported="false" />

        <receiver
            android:name=".receiver.SearchReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.content.movie.search.Action" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".receiver.CustomWebReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.content.movie.custom.web.Action" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="design_width_in_dp"
            android:value="1280" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="720" />
        <!-- P2 播放宿主 + P3 媒体通知(FGS mediaPlayback;引擎与通知共用同一实例) -->
        <service
            android:name=".player.PlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />
    </application>

</manifest>
